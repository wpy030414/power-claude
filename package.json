{
  "name": "power-claude",
  "private": true,
  "version": "0.1.0",
  "description": "PowerClaude — Terminal + Claude Code 一键美化配置方案",
  "type": "module",
  "scripts": {
    "apply": "tsx src/index.ts",
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "tsx --test \"tests/**/*.test.ts\""
  },
  "dependencies": {
    "@clack/prompts": "^0.10.1",
    "picocolors": "^1.1.1"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}